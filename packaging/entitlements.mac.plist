<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron/V8 needs JIT + writable-executable memory under hardened runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Loading native modules (better-sqlite3, node-pty) signed separately. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- arc spawns electron-as-node + agent CLIs via env vars (ELECTRON_RUN_AS_NODE). -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Child processes inherit these entitlements. -->
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
